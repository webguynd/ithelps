<%- include("partials/header") %> 

<div class="container">
    <table class="table">
        <thead>
            <tr>
                <th>Title</th>
                <th>Device</th>
                <th>Posted By</th>
                <th>Details</th>
            </tr>
        </thead>
        <tbody>
            <% ticket.forEach(t => { %>
                <tr>
                    <td><%= t.title %></td>
                    <td><%= t.device %></td>
                    <td><%= t.postedBy %></td>
                    <td><a href="/api/tickets/<%= t._id %>">More Details...</a></td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</div>

<%- include("partials/footer") %>